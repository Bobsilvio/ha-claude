flask==3.0.0
flask-cors==4.0.0
waitress==3.0.2
requests==2.31.0
python-dotenv==1.0.0
anthropic>=0.39.0
openai>=1.40.0
google-genai>=1.0.0
websocket-client>=1.6.0
pyyaml>=6.0
# Voice: uses OpenAI Whisper API / Google Gemini / HA STT (no extra deps needed)
# File upload features (optional)
PyPDF2
python-docx
# RAG features (optional, auto-detected)
# Note: torch and sentence-transformers not available in Alpine Linux
# Install manually if needed: docker exec -it ha-claude pip install torch sentence-transformers
